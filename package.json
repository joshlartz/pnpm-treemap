{
  "name": "pnpm-treemap",
  "version": "1.0.0",
  "description": "Generate a self-contained HTML treemap of a pnpm lockfile",
  "type": "module",
  "bin": {
    "pnpm-treemap": "./pnpm-treemap.mjs"
  },
  "scripts": {
    "start": "node pnpm-treemap.mjs"
  },
  "dependencies": {
    "@pnpm/lockfile-file": "^9.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
